=====================
\`\`hello\`\` Hello World
=====================

这是标准的 "Hello, World" 示例。它基本上就是 ``examples/null`` 加上一条 ``printf`` 语句。主要用于验证新的 NuttX 架构能否正常工作。

- ``CONFIG_NSH_BUILTIN_APPS`` – 将 "Hello, World" 示例构建为 NSH 内置应用程序。
